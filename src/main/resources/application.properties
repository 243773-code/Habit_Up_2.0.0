# La URL usa localhost porque MySQL está en la misma máquina de AWS
spring.datasource.url=jdbc:mysql://localhost:3306/habit_up_2?createDatabaseIfNotExist=true&serverTimezone=UTC

# Credenciales que te pasó Alejandro
spring.datasource.username=root
spring.datasource.password=root

# Magia de Hibernate: crea las tablas por ti
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Puerto de la app
server.port=8080ies.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect